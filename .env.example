# Optional — AI summaries use mock enrichment when unset
GEMINI_API_KEY=

# Optional — search results persist when unset
MONGODB_URI=
MONGODB_DB=opportunity_scout

# Provider integrations (implementation details)
GREENHOUSE_BOARD_TOKEN=
LEVER_COMPANY_SLUG=
