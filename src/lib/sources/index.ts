/**
 * @deprecated Use @/lib/providers instead.
 * Kept for backwards compatibility during architecture migration.
 */
export {
  MockProvider as MockEmployerSource,
  type OpportunityProvider as EmployerSource,
} from "@/lib/providers";
